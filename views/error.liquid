{% render 'partials/head.liquid' %}
{% render 'partials/nav.liquid' %}

<main class="container">
  <section class="error-pagina">
    <h1>{{ statusCode }}</h1>
    <p>{{ message }}</p>
    <a href="/" class="btn-ronde">Terug naar home</a>
  </section>
</main>

{% render 'partials/foot.liquid' %}